{
  "build": {
    "builder": "NIXPACKS_PYTHON_DJANGO"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "python manage.py migrate && gunicorn fashion_site.wsgi --bind 0.0.0.0:$PORT"
  }
}